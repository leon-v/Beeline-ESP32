{
    "name" : "Device Timer",
    "description": "Creates a device wide interval timer to trigger message sources to perform a reading.",
    "nvsKey": 1,
    "variables":{
        "Interval": {
            "inputType": "number",
            "inputUnits": "ms",
            "type": "integer",
            "bits": 32,
            "unsigned": true,
            "default": 5000,
            "description": "Interval in milliseconds"
        }
    }
}